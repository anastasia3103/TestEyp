<Page x:Class="TestEyp.Pages.TeacherPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TestEyp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="350" d:DesignWidth="800"
      Title="TeacherPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
            <RowDefinition Height="20" />
            <RowDefinition Height="50" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Светлана Дмитриевна, введите пароль:"
                   Grid.Row="1" FontSize="25" 
                   HorizontalAlignment="Center"/>
        <PasswordBox Grid.Row="2" FontSize="25" Width="450"
                   HorizontalAlignment="Center"
                     Name="PasswordPb"/>
        <Button Grid.Row="4" FontSize="25" Width="100"
                   HorizontalAlignment="Center" Name="OkBtn"
                FontWeight="Bold" Background="LightBlue"
                Click="OkBtn_Click" Content="OK" />
    </Grid>
</Page>
